Процедура ОбработкаПроведения(Отказ, РежимПроведения)
    Попытка
        VKRHR.ПровестиИсточник(ЭтотОбъект, Перечисления.TipyKadrovykhIzmeneniy.Priem);
    Исключение
        Отказ = Истина;
        Сообщить(ОписаниеОшибки());
    КонецПопытки;
КонецПроцедуры
