// Серверный модуль с разрешённым вызовом сервера, без привилегированного режима.
Процедура ВыполнитьЦикл() Экспорт
    Если НЕ ПравоДоступа("Изменение", Метаданные.РегистрыСведений.VKROperatorDecisions) Тогда
        Возврат;
    КонецЕсли;
